[phases.setup]
nixPkgs = ["chromium", "chromedriver", "tesseract", "python311"]
 
[phases.install]
cmds = ["pip install -r requirements.txt"]
 
[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1"